server:
  port: 7030
  servlet:
    context-path: /click

spring:
  application:
    name: click


  redis:
    host: localhost
    port: 6379
    password: "123456"
    timeout: 1000
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: 1
